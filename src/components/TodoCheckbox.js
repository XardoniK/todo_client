import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {};

const defaultProps = {};

const TodoCheckbox = (props) => {

	return (
		<>

		</>
	);
};

TodoCheckbox.propTypes = propTypes;
TodoCheckbox.defaultProps = defaultProps;

export default TodoCheckbox;
